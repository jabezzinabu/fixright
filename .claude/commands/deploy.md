Stage all changed files, commit with the message "$ARGUMENTS", and push to origin main.

Steps:
1. Run `git status` to see what has changed
2. Stage relevant files (do not use `git add -A` — add files explicitly by name)
3. Commit with the provided message, appending the Co-Authored-By trailer
4. Push to origin main
5. Confirm the push succeeded and show the commit hash

Do not push if there are no changes to commit.
